services:
  backend-api:
    build:
      context: ./backend
      dockerfile: Dockerfile  # 🟢 Coincide con tu "F" mayúscula real
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://ep-odd-heart-apsgp42x-pooler.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require
      - SPRING_DATASOURCE_USERNAME=neondb_owner
      - SPRING_DATASOURCE_PASSWORD=npg_Vte7lN3mKJxA
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
    restart: always

  frontend-dashboard:
    build:
      context: ./frontend
      dockerfile: Dockerfile  # 🟢 Coincide con tu "F" mayúscula real
    ports:
      - "3000:3000"
    restart: always
    depends_on:
      - backend-api