spring.application.name=pos

# ============================================================================
# NEON POSTGRESQL CONNECTION CONFIGURATIONS
# ============================================================================
# Neon PostgreSQL connection settings for project steep-grass-09339871
spring.datasource.url=jdbc:postgresql://ep-odd-heart-apsgp42x-pooler.c-7.us-east-1.aws.neon.tech/neondb?channel_binding=require&sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_Vte7lN3mKJxA
spring.datasource.driver-class-name=org.postgresql.Driver

# ============================================================================
# JPA & HIBERNATE ORM CONTROLS
# ============================================================================
# Forces Hibernate to compile optimized PostgreSQL dialect SQL syntax
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Since you are manually running your 9-table script directly inside Neon,
# set this to 'validate' so Spring simply checks that your tables exist.
spring.jpa.hibernate.ddl-auto=validate

# Set to true to print every automated query directly into your VS Code terminal console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true